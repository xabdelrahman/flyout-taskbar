<Window
    x:Class="FluentMusic.Views.HandleWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    mc:Ignorable="d"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006">

    <Grid
        x:Name="Root"
        Background="Transparent"
        ContextFlyout="{StaticResource HandleMenu}"
        PointerPressed="OnPointerPressed"
        PointerMoved="OnPointerMoved"
        PointerReleased="OnPointerReleased"
        PointerEntered="OnPointerEntered"
        PointerExited="OnPointerExited">

        <Grid.Resources>
            <MenuFlyout x:Key="HandleMenu">
                <MenuFlyoutItem Text="Settings" Click="OnSettingsClick" />
                <MenuFlyoutSeparator />
                <MenuFlyoutItem Text="Exit" Click="OnExitClick" />
            </MenuFlyout>
        </Grid.Resources>

        <Border
            x:Name="Pill"
            Background="{ThemeResource GlassScrimBrush}"
            BorderBrush="{ThemeResource GlassRimBrush}"
            BorderThickness="1"
            CornerRadius="8">
            <Border.RenderTransform>
                <ScaleTransform x:Name="PillScale" CenterX="14" CenterY="33" />
            </Border.RenderTransform>

            <FontIcon
                x:Name="Glyph"
                FontFamily="{ThemeResource SymbolThemeFontFamily}"
                Glyph="&#xE8D6;"
                FontSize="15"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
        </Border>
    </Grid>
</Window>
